import { Guid } from "@omnia/fx-models";

export interface EkmanBanner {
  id: number;
  linkHeader: string;
  linkUrl: string;
  header: string;
  content: string;
  onHover: string;
  textColor: string;
}
